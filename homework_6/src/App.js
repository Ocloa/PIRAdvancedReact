import React from "react";
import Blender from "./context/BlenderProvider";

// const WrappedColorDropdown = StyleWrapper(ColorDropdown);
// const WrappedResultDisplay = StyleWrapper(ResultComp);
// const WrappedGroupedResultDisplay = StyleWrapper(GroupedComp);

const App = () => {
  return (
    <Blender>
    </Blender>
  );
};

export default App;